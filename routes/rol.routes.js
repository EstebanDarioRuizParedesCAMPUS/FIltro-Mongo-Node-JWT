const { Router } = require('express')
const { check } = require('express-validator')

const router = Router()

router.get('/',[],)
router.post('/',[],)
router.delete('/:id',[],)
router.patch('/:id',[],)
router.get('/id',[],)

module.exports = router